{
  "name": "@agentsforx/mcp",
  "version": "0.1.0",
  "description": "MCP server for Agents For X — generate, draft, schedule, and publish X posts and replies in your voice, plus analytics.",
  "license": "UNLICENSED",
  "type": "module",
  "engines": { "node": ">=20" },
  "bin": {
    "agentsforx-mcp": "dist/stdio.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/stdio.js",
    "inspector": "npm run build && npx @modelcontextprotocol/inspector node dist/stdio.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
